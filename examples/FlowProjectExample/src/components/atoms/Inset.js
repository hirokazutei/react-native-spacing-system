/* @flow */
import {insetFactory} from 'react-native-spacing-system';
import {spacing, type Spacing} from '../../constants/spacing';

const inset = insetFactory<Spacing>(spacing);

export default inset;
